import RecommendationsView from "@/components/RecommendationsView";

export default function RecommendationsPage() {
  return <RecommendationsView name={null} email={null} avatarUrl={null} isLoggedIn />;
}
